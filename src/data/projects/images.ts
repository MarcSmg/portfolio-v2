export interface ProjectImages {
    cover: string,
    gallery?: string[]
}

export const projectImages: Record<string,ProjectImages> = {
    fretwise: {
        cover: "",
        gallery: []
    }
}